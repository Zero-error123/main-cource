"use strict";

// 공개 프로필과 동작 기준을 한곳에서 관리합니다. 비밀 키를 넣지 마세요.
const PORTFOLIO_CONFIG = Object.freeze({
  githubUsername: "Zero-error123",
  cacheMinutes: 10,
  requestTimeoutMs: 12000,
  headerScrollThreshold: 60,
  backToTopThreshold: 300,
  revealThreshold: 0.2,
});
